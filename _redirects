# NÃO mexer em assets estáticos
/js/*     /js/:splat     200
/css/*    /css/:splat    200

# SPA fallback (apenas para rotas do app)
#/app/*   /app.html      200   ← se você usa /app
/*        /index.html    200